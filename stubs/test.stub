<?php

declare(strict_types=1);

namespace {{ namespace }};

use App\Models\User;
use Database\Factories\UserFactory;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

class {{ class }} extends TestCase
{
    use RefreshDatabase;

    public function test_(): void
    {
        $user = UserFactory::new()->createOne();

        \assert($user instanceof User);

        $response = $this->be($user, 'users')->getJson(resolveUrlFactory()->action(Controller::class));

        $response->assertOk();

        $this->assertJsonApiResponse($response, $this->jsonStructureResource());
    }
}
